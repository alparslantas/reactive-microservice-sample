server:
  port: 8888
  
management:
  security:
    enabled: false
    
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:configs/